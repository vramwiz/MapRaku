# ちずらく 仕様検討まとめ

## 1. 位置づけ

仮称：**ちずらく**

目的は、本格GISではなく、案内図・略地図を簡単に作成するためのベクター地図作成アプリとする。

参考イメージは、JustSystemsの「地図スタジオ」のように、実際の地形や道路形状をそのまま忠実に再現するのではなく、必要な要素だけを残し、直線化・直交化・省略を行って、見やすい案内図を作成する方向とする。

既存の類似アプリとして、
- MIF互換を重視したベクターアプリ
- AviUtl2向けレイアウト作成アプリ／アプリ版

が存在する。

MIF互換アプリはSVG対応済みであるが、MIF互換に特化し、それ以上の機能を持たせない方針のため、地図機能は直接追加しない。

ちずらくは、既存レイアウトアプリ側の描画・選択・キャンバス・保存などの仕組みを流用しつつ、地図専用アプリとして構成する方向とする。

---

## 2. 初期開発方針

最初から地図一般を網羅しない。

今回想定している駅周辺の略地図を描ける程度の機能を初期仕様とし、実際に地図を作成しながら不足機能を都度追加する。

凝りすぎず、必要十分な機能を優先する。

---

## 3. 地図の基本表現方針

実際の方位や形状を忠実に再現することは目的としない。

今回の例では、
- 北西方向を画面上側にする
- 線路は左右方向のほぼ水平な直線として配置する
- 道路も可能な限り0度・45度・90度など、整理された角度で表現する
- 駅の片側を中心に描き、反対側は必要最低限に省略する

といった略図化を基本とする。

そのため、画面上方向が必ずしも北ではない。

---

## 4. 方位表示

地図には方位を示す機能を用意する。

最低限、
- 北方向を示す記号
- 「北」の文字表示

を配置できるようにする。

将来的には地図全体に基準方位角を持たせ、北記号の向きを連動させることも可能とする。

---

## 5. UI構成

### 5.1 基本構成

- 上部：ファイルメニューなど
- 中央：地図編集キャンバス
- 左側：配置ツール・地図要素
- 必要に応じて選択中オブジェクトの設定UIを表示

左側のツールは分類して整理する。

例：
- 道路・線路
- 水系
- 図形
- 記号
- 文字
- 建物

四角、角丸四角などの一般図形も「図形」分類に含める。

---

## 6. 選択モード

明示的に何も作成ツールが選択されていない状態を、選択モードとする。

道路などの作成後、自動的に選択モードへ戻るのではなく、道路作成を明確に終了する操作が行われるまで道路作成モードを継続する。

ダブルクリックまたは右クリックで連続道路配置を終了すると、道路ツールの選択が解除され、通常の選択モードへ戻る。
再度道路ツールを選んで配置した道路は、別グループとする。

---

## 7. キャンバス操作

- マウスホイール：拡大・縮小
- オブジェクトがない部分を左ドラッグ：矩形範囲選択
- 中央ボタンドラッグ：常時キャンバス移動

道路・線路作成中は左クリック・左ドラッグを作図操作に使う。
中央ボタンドラッグによるキャンバス移動は、作図中も可能とする。

---

## 8. スナップ

スナップは常時ONとする。

一時的にスナップを解除したい場合は、**Altキーを押しながら操作する**。

ON/OFF用の通常UIは持たせない。

---

## 9. オブジェクト管理

以下は既存アプリの仕組みを流用する。

- Undo / Redo
- 複数選択
- コピー
- 複製
- 削除
- グループ化
- グループ解除
- 表示順変更
- キャンバスサイズ

グループは地図でも必要とする。

例：
- 駅＋駅名＋駅周辺要素
- 建物＋名称
- 複数の案内記号
- 一体として移動したい地図要素

---

## 10. 層

層は既存のレイヤー順とグループ構造で扱い、独立した層番号の管理UIは設けない。
連続して配置した道路は同層・同グループとし、上下関係はレイヤー順に従う。
道路を範囲選択などで複数選択し、メニューから後で同層・同グループにまとめられる。
グループ内の個別道路をグループから外す操作も用意する。
道路グループはクリックで全体選択、ダブルクリックで内部の個別道路・点を編集する。

### 10.1 同層

同じ層の道路同士は、接触・交差時に継ぎ目なく接続して見えるよう描画する。

同じ層の線路同士も、接続・分岐対象となる。

各経路は独立したオブジェクトとして保持する。スナップで位置を合わせるが、永続的な接続関係や、移動時に他の経路を追従させる拘束は持たせない。
接続の描画は現在の形状から判定し、編集で離れた経路は分離したままでよい。

### 10.2 異層

異なる層同士は、画面上で交差しても接続しない。

例：
- 道路と道路 → 高架・立体交差
- 道路と線路 → オーバーパス／アンダーパス
- 道路と川 → 橋

層の上下関係はグループのレイヤー順を使用する。グループの識別に必要な内部IDは保持してよい。

---

## 11. 異層交差

異層で重なる場合は、単純に橋だけではなく、複数の表現を想定する。

候補：
- 自動
- 高架
- 橋
- トンネル
- 非接続
- 表現なし

初期実装では必要最低限から開始し、必要に応じて追加する。

---

## 12. 道路

道路は中心経路を持つオブジェクトとして扱う。

### 12.1 基本機能

- 直線
- カーブ
- 複数点による経路
- 道路同士の接続
- 分岐
- 層
- 太さ
- 末端形状

### 12.2 太さ

道路の色は層単位や全体設定で共通化できるが、道路幅は道路ごとに異なるため、太さはオブジェクト単位で保持する。

### 12.3 末端形状

既存アプリと同様に、
- 四角
- 丸
- 三角

から選択可能とする。

道路同士が接続した部分では末端形状より接続表現を優先する。

### 12.4 将来の道路スタイル

将来的に、
- 2車線道路
- 中央線あり
- 中央分離帯あり

などを描画できるようにする。

道路本体は単なる太線ではなく、「中心経路＋道路スタイル」として拡張可能な構造を想定する。

交差点部分では、中央線などの車線表現は自動的に途切れ、交差部分には何も描かない状態でよい。

---

## 13. 道路・線路の点編集

道路・線路を選択すると、経路上の点を表示する。

選択中の道路・線路は、そのまま点編集可能とする。

### 13.1 点追加

道路・線路が選択されている状態で、点と点の間の線分部分を左クリックすると、新しい点を追加する。

### 13.2 点削除

道路・線路が選択されている状態で、既存の点上を右クリックすると、その点を削除する。

### 13.3 右クリック

点削除条件に該当しない右クリックは、通常のポップアップメニューを表示する。

---

## 14. 曲線

道路、線路、川はカーブを持てるようにする。

参考アプリの連続線と同様、点ごとに鋭角／ベジェを切り替え、1本の経路内に直線と曲線を混在できる。
共通の頂点列・制御点・区間種別を保持し、点編集は既存アプリの仕組みを流用する。
要素の種類として道路・線路・川を区別し、それぞれ専用の描画を行う。

---

## 15. 線路

### 15.1 基本機能

- JR
- 私鉄
- 直線
- カーブ
- 分岐
- 駅へのスナップ
- 層

線路も道路と同様に、複数点による経路オブジェクトとする。

### 15.2 複数線路

実際の駅周辺では多数の線路が並行する場合がある。

案内図では実際の線路本数を忠実に描かず、必要に応じて簡略化する。

将来的には、
- 単線
- 複線
- 複数線路をまとめた鉄道帯

などの表現も検討する。

---

## 16. 駅

駅は単純な四角図形だけでも表現できるが、線路とのスナップや一体的な扱いが必要になる可能性があるため、専用オブジェクト化を検討する。

初期段階では最低限、
- 四角形などによる駅表現
- 線路へのスナップ
- 駅名文字

を使用できればよい。

駅名は一般の文字オブジェクトとして扱ってよい。

---

## 17. 横断歩道

横断歩道は道路内部の属性ではなく、**独立したオブジェクト**とする。

道路へのスナップを使用して配置する。

配置時に、
- 最寄り道路へ吸着
- 道路方向に対して直角に回転
- 道路幅に合わせて長さを調整

できるとよい。

初期実装では「配置時スナップ」のみでもよく、道路変更後の自動追従は必須としない。

---

## 18. 信号機

信号機は通常の地図記号オブジェクトとして扱う。

道路・交差点付近へのスナップが可能であればよい。

---

## 19. 歩道橋

歩道橋は専用オブジェクトを想定する。

最低限、
- 道路を跨ぐ橋部分
- 両端の階段
- 階段と分かる程度の簡略表示

を持たせる。

道路に対してスナップできるとよい。

---

## 20. 水系

### 20.1 川

川は道路・線路と同様に、経路を持つ帯状オブジェクトとして扱う。

- 直線
- カーブ
- 幅
- 分岐
- 合流
- 層

を想定する。

### 20.2 海・湖・池

海、湖、池は閉じた面オブジェクトとして扱う。

- 自由形状
- ベジェ
- 塗り
- 輪郭線

を持たせる。

---

## 21. 建物

建物は通常の図形オブジェクトとして配置できるようにする。

主に、
- 四角
- 角丸四角
- その他の閉じた図形

を利用する。

必要に応じて建物名などの文字を組み合わせ、グループ化できるようにする。

---

## 22. 文字

文字は一般オブジェクトとして扱う。

駅名、道路名、施設名、案内文字などに使用する。

既存アプリの文字処理を基本的に流用する。

---

## 23. 配色

道路や線路ごとに個別色を設定することを基本とはしない。

色は地図全体や層単位で共通管理する。

### 23.1 基本テーマ

最低限、
- 白ベース
- 黒ベース

の2種類のプリセットを用意する。

白ベースでは道路・線路・文字などを黒系、
黒ベースでは道路・線路・文字などを白系にする。

### 23.2 一括変更

作成済みの同じ地図に対して、配色プリセットを変更することで一括してベース色を変更できるようにする。

印刷用、画面表示用などで同じ地図を使い分けられるようにする。

### 23.3 層共通色

同じ層の道路・線路などは共通色として扱ってよい。

将来的に必要になった場合のみ、個別色上書きを追加する。

---

## 24. 画像配置

画像の配置を許可する。

画像そのものをJSONに埋め込まず、外部ファイルへの参照として保持する。

参照画像が見つからない場合、その画像オブジェクトは削除する。

---

## 25. 保存形式

編集データはJSON形式とする。

JSON内には最低限、
- アプリ識別ID
- フォーマットバージョン
- キャンバス情報
- 配色設定
- 層情報
- オブジェクト情報
- グループ情報
- 画像参照情報

を持たせる。

アプリ識別IDやデータ構造が異なるJSONは読み込まない。

無理な互換読込は行わない。

---

## 26. SVG出力

最終出力はSVGを基本とする。

編集用JSONとSVGは役割を分ける。

- 編集用：JSON
- 外部出力：SVG

SVG出力時は、参照画像を外部参照のまま残さず、画像データをSVG内へ埋め込む方向とする。

これにより、SVG単体で持ち運べる状態にする。

---

## 27. 初期実装の考え方

今回の駅周辺略地図を作成できる程度までを初期実装の目標とする。

初期段階で重要なのは、
- 道路
- 線路
- 駅
- 建物
- 文字
- 横断歩道
- 信号機
- 歩道橋
- 川・池・海
- 層
- 異層交差
- スナップ
- グループ
- 配色プリセット
- JSON保存
- SVG出力

とする。

それ以外の細かな機能は、実際の地図を作成し、不足が明確になった時点で追加する。

---

## 28. 会話で確定した開発方針（2026-09-16）

- 作業再開用の `note.md` と、利用者向け説明書 `README.md` をMapRakuにも用意する。開発・仕様変更に合わせて更新し、予定と実装済みの機能を区別する。

- ユーザー提供の千里丘の店舗略地図を初期機能の具体例とする。右側のCodexキャラクターは対象外。
- 地図作成に必要十分な機能を備える。参考画像の見た目そのものや店舗ロゴの再現を必須とはしない。
- 主な土台は `D:\DelphiProg\test\SYNC_ScreenLayout` の単独アプリ側とする。
- `D:\DelphiProg\test\VectArtDesigner` はSVG出力などの流用元とする。両アプリのモデル差に合わせて移植する。
- 制作先はMapRakuとし、参照元アプリのソースは変更しない。
- 道路・線路・川などの種類ごとにデータ・専用描画の担当ユニットを分離する。共通経路編集、接続形状計算、UI、保存を種類別ユニットへ集中させない。
- 通常のまとめグループと、経路を同層として描くグループは内部属性で区別する。駅＋文字などをまとめても、道路の層が意図せず結合しない構造にする。
- 以下の補完方針は、残りの通常判断を任せるというユーザー指示に基づく実装既定値であり、個別に回答を得た事項とは区別する。

## 29. 未指定部分の実装既定値

### 29.1 入力・編集

- Ctrl＋クリックで選択の追加・解除。矩形範囲選択とグループ内部編集は既存操作を活用する。
- 作図中の右クリックは配置終了を優先し、選択モードでは経路の点削除、それ以外はメニュー表示とする。
- スナップは端点・頂点・経路上・整列・整理された角度を対象とし、画面上の一定距離で判定する。Alt中は解除する。
- 同層へまとめる操作とグループから外す操作は、位置と経路形状を保持し、Undo／Redo対象にする。

### 29.2 地図表現

- 異層交差はレイヤー順に重ね、上側の道路の縁取り等で区別する。橋・トンネル記号は初期段階では手動配置とする。
- 駅は線路へスナップできる矩形と一般文字を基本とする。線路変更後の追従は持たせない。
- 横断歩道は配置時に道路へスナップし、方向・長さを道路に合わせる。配置後は独立して編集する。
- JRは交互の帯模様、私鉄は線と枕木による略図表現を基本とする。
- 信号、番号付き丸印、国道・県道番号、矢印、方位、駐車場などの基本記号を用意する。固有の店舗ロゴは画像配置で扱う。
- 文字は既存処理を基本に、縁取り・回転・縦書きなど参考地図に必要な表現を用意する。
- 白／黒テーマは背景・道路・基本文字などの役割別配色へ適用する。水系・記号のアクセント色と画像の固有色は保持する。

### 29.3 保存・出力・追加機能

- 編集ファイルは専用拡張子 `.mapraku` のJSONとし、アプリ識別IDとバージョンで検証する。
- 画像参照は文書位置からの相対パスを基本とし、相対化できない場所では絶対パスを使用する。別名保存では参照先を維持する。
- 不明な形式や不正データは現在の文書を破壊せず拒否する。画像参照が失われた場合は該当オブジェクトを除外し、件数を知らせる。
- SVGを主出力とし、画像を埋め込む。文字は見た目を維持するアウトライン出力を基本にする。
- PNG出力、整列・均等配置、回転・反転、表示／ロック、入れ子グループ、画像D&D、最近使ったファイルを流用候補として採用する。
- 実装検証では道路の同層描画・別層描画、点ごとの曲線切替、グループの統合と分離、Undo／Redo、JSON往復、SVG出力を重点確認する。
- 通常の細部は既存アプリと本仕様に沿って判断し、制作を左右する重要な矛盾・不足が判明した場合だけ質問する。
