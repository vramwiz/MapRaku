# ちずらく 開発ノート

## 現在地（2026-09-17）

### 共通基礎クラスへ集約する方針（2026-09-17 ユーザー指定）

道路、線路などのオブジェクトごとに同じ処理を実装しない。端点、接線、接続可否など経路共通の機能は基礎クラス `TVectArtPathLayer` に実装し、種類・系列の情報だけで適用対象を決める。今後の処理も共通の基礎クラス／共通処理へ集約し、コードの肥大化を防ぎ、不具合修正や調整が1か所で済む構成を維持する。種類固有の差だけを共通の種類判定や拡張点へ追加する。

端点接続は配置と頂点ドラッグの両方に実装済み。

- 同系列の始点・終点を線上の途中より優先してスナップする。画面8pxの判定距離は `MapRakuPathSnap` の共通定数に集約。確定時は小数を含む実際の端点座標へ合わせる。
- 道路→道路、線路→線路、川→川に適用。JRと私鉄は線路系列を共有する。対応は基礎クラスの `ConnectionFamilyOf` に集約し、異系列へは自動接線調整しない。
- 直線とベジェの接続では曲線側の制御点の長さを保って方向をそろえる。曲線を動かす場合はその曲線、直線を動かす場合は相手の曲線を調整する。曲線同士は操作側を相手の接線へ合わせる。直線同士は端点位置を合わせる。
- 両端を接続する新規経路は、両端の位置確定後に角度を調整する。区間の直線／曲線種別は維持する。ゼロ長ハンドルは隣接区間から長さを補う。
- 基礎クラスの `TryEndpoint`／`ConnectEndpoint`／`AlignEndpointTangent` を配置・編集から共用。複数経路の変更は共通の `TMapRakuPathEditSession` で一度にUndo／Redoする。ドラッグで候補から離れた場合は、候補側への仮の角度変更を戻す。
- Altでスナップ・自動角度調整を解除する。ロックされた相手（祖先グループを含む）は位置参照のみとし、形状は変更しない。非表示・閉じた経路は端点候補にしない。
- 接続はその操作時の位置・接線調整であり、経路は独立オブジェクトのまま。後から離した際に相手が追従する永続的な拘束は作らない。

回帰テスト：始点／終点の全組合せ、各系列、配置順、頂点ドラッグ、両端接続、ズームと小数座標、変形された相手、ゼロ長制御点、ロック／非表示グループ、Alt、Undo／Redo、JSON。描画比較は `TestOutput/connection-examples.png`、編集可能な例は同名の `.mapraku`。

### 近接する橋の連結（2026-09-17 ユーザー指定）

同じ上側経路にある橋区間が重なる、または端の開きが衝突するほど近い場合は、側線を一続きに描く。中間の端の開きは省き、連結区間の両外端だけに付ける。離せば自動的に別々の橋へ戻る。

- 共通幾何処理 `AddMapBridgeSpan` で経路上の距離区間を統合する。道路／JR／私鉄ごとに重複実装しない。連結距離は共通定数 `MAP_BRIDGE_JOIN_GAP = 10`（地図座標）に集約。
- 同じ上側オブジェクト・同じ側線形式だけを統合し、3個以上の連鎖もまとめる。通常の橋形式とアンダーパス形式は別々に扱う。踏切・トンネルは対象外。
- 下側の切り抜きは交差対象ごとに保持し、連結後も下側の道路・線路を必ず途切れさせる。
- 画面／PNG／SVGを共通描画で検証済み。半透明側線の重複、内側の開き、切り抜き、移動による分離、異なる経路・形式、連鎖統合を回帰テストする。例は `TestOutput/joined-bridges.png`／`.svg`／`.mapraku`。

### 最重要：立体交差の下側は必ず途切れさせる（2026-09-17 ユーザー指定）

道路同士・道路と線路のオーバーパス、アンダーパス、橋、跨線橋、トンネルでは、下側オブジェクトを奥に連続描画して上側で覆う方式に戻さない。上側の本体と橋・トンネル記号が占める交差区間に余白を加え、下側の線・縁・模様を描画対象から除外する。

- 道路が線路より上なら線路を切る。道路が線路より下なら道路を切る。道路同士も同様。
- 上側は元の経路のまま連続させ、下側だけを切り抜く。背景色の白塗りや合成済み画面の消去で代用しない。透明背景、黒地図、半透明の上側でも下側を残さず、無関係の下層図形は消さない。
- 平面の道路交差は継ぎ目なく接続する。踏切は線路模様を優先し、立体交差の余白や橋記号を付けない。
- 参考：[ゆびさきクラブ「踏切の地図の書き方」](https://yubisakiclub.com/archives/1870)。オーバーパス・アンダーパス・坑道の図を確認した。上記の必須ルールはユーザーの明示指定。

実装：交差ごとの下側クリップと上側記号を共通描画へ組み込み、PNG／SVGも画面と同じ高さ区間・道路の縁→本体順で描く。線幅・交差角度・曲線から切り抜き区間を求める。SVGCanvasのDifferenceクリップ出力には反転不備があるため、残す領域をSkiaのパス差分で生成してIntersectクリップする。グループ内経路も交差対象とし、地図経路を動かす間は古い移動画像キャッシュを使わず再計算する。

右クリック「交差部分」にアンダーパス／トンネルを追加。上下は「選択1／2を上にする」で指定する。既存の保存値を変えず列挙値を末尾へ追加し、Undo／RedoとJSONを共用する。

回帰テスト：道路／JR／私鉄の下側、5種の立体交差、逆順レイヤー、半透明上側、非表示・移動後、グループ、太い斜交・曲線・黒地図を検証。PNGとSVGを実際に読み戻して透明な切れ目を検査する。Win64 Debug／Releaseは警告・エラー0でビルド済み。比較用の編集データ・PNG・SVGは `TestOutput/crossing-examples.*`、曲線例は `TestOutput/crossing-curved*.png`。

Delphi/VCLの初期実装を作成し、Win64 Debug／Releaseをビルド済み。仕様全体の完成版ではない。通常の未指定部分は実装側で判断し、重要な矛盾のみ質問する。

主な基盤は D:\DelphiProg\test\SYNC_ScreenLayout から複製。参考元は変更していない。アプリ名、保存形式識別子、自動操作接続名はMapRakuへ分離した。旧基盤由来の未使用プラグインユニットや履歴ファイルは残るが、単独アプリとしてビルドする。

## 今後も参照するアプリ

`D:\DelphiProg\test\SYNC_ScreenLayout` にある次の2プロジェクトを、今後もUI、編集操作、描画、保存、Undo／Redo等の実装参考にする。参照元は変更せず、MapRaku側へ必要な考え方や実装だけを取り込む。

- `ScreenDesignMaker.dpr`／`ScreenDesignMaker.dproj`：同じ編集画面を単独起動するデバッグアプリ。ファイル操作、編集UI、キャンバス操作の参考元。
- `SYNC_ScreenLayout_Filter.dpr`／`SYNC_ScreenLayout_Filter.dproj`：AviUtl2用の画面レイアウトプラグイン。ホスト連携を含む構成だが、共通のモデル、描画、編集コマンド、操作処理の参考元。

両プロジェクトは同じフォルダーと多くの `Source` ユニットを共有する。MapRakuは単独アプリとしての分離を維持し、プラグイン固有のホスト連携は持ち込まない。

## 実装済み

- 地図パネルは分類コンボと、実際の配置形状を示すサムネイル一覧で構成。道路・JR・私鉄・川は直線／鋭角連続線／ベジェ連続線を選び、池は円・四角・角丸・閉じたパス等を既存作成ツールで配置する。記号も同じ一覧から直接配置し、追加ボタンを持たない。
- 道路・線路・川は独立した通常レイヤーとして追加する。編集画面とレイヤー一覧で共有する右クリックメニューの「高さの区切りを追加」で高さの区間を分け、区切り間を同層として扱う。グループは整理専用で高さを意味しない。
- グループ内で選択された地図層は子孫の経路を線幅ツールバーへ展開する。複数経路を含む選択では全頂点を表示し、クリックした頂点・区間から編集対象の経路を自動決定する。
- 頂点上のU／Vインターフェイスは削除。参考元のPath操作に合わせ、選択頂点でPを押すと鋭角／ベジェを切り替える。キャンバス設定の白地図／黒地図選択はオーナー描画のダークコンボを使用する。
- 地図経路の頂点当たり判定は14pxへ拡大し、線分分割の7px判定より優先する。ホバー中の頂点は橙色の大きなハンドルで表示する。
- 線幅は右上の既存数値欄・スライダーへ統一し、線端方式は既存の「詳細」で選択。頂点はU＝ベジェ、V＝鋭角へ変更し、選択点へ現在のキーを表示。
- グループ化・グループ解除・子の取り外しは通常の整理操作として残す。白／黒配色をキャンバス設定画面へ移動。
- 共通連続線に道路／JR線／私鉄線／川の種類を保持し、種類別ユニットで描画。
- 高さの区切り間の道路外縁を先にまとめて描き、その後本体を描いて同層交差部を接続。区切りを挟む区間は別々に合成して立体交差を表現する。
- 全レイヤーは保存後も変わらない永続IDを持つ。交差計算は道路・線路・川の現在形状とIDを参照し、通常交差／踏切／高架／橋／跨線橋を従属関係として導出する。直線とベジェの両方を対象とし、編集後は固定座標を使わず再計算する。
- 道路1本と複数線路の交差は道路を主体とする交差グループへまとめる。交差が消えた対象は再計算結果から外す。地図経路を2本選択すると、共通右クリックメニューの「交差部分」から通常交差／踏切／高架／橋／跨線橋／表現なし、上下関係、短い／標準／長い表現範囲を上書きできる。指定はUndo／RedoとJSON保存に対応する。交点周辺の経路へ専用表現を描く。交差と下側の切り抜きは個別に計算し、同じ上側経路の近接する橋側線は連結する。画面・PNG・SVGは同じベクター描画を使用する。ドラッグによる連続範囲調整は次段階。
- 歩道分類の階段は既存Pathを再利用する独立オブジェクト。上り／下りを種類として保持し、始終点から向きと長さ、線幅から幅、長さから段数を導出する。段数は自動または右クリックから6／10／16段を指定でき、Undo／RedoとJSON保存に対応する。歩道橋は2点配置から始点側階段・橋本体・終点側階段の3要素グループを生成し、各要素を既存のグループ内Path編集で変更できる。
- JR線の見た目は従来の帯本体＋反対色点線を維持する。新規配置時とファイル読込時にキャンバスの白／黒配色へ線色を正規化し、背景と外帯が同色になって点線だけ見える状態を防ぐ。
- 層境界から自動判定した高架・立体交差も、明示指定した橋・高架・跨線橋も、下側を記号と余白の範囲で切り抜く。上側は元の経路を連続描画し、外側に橋の側線を追加する。短い道路・線路を上描きする旧処理は廃止した。
- 同じ上側経路の近接する橋側線は連結する。各交差を個別に計算し、下側に複数の切り抜きがあればすべて適用する。踏切を一括表示する処理は停止中。
- 複数経路のグループ化、解除、子の取り外しとUndo／Redo。
- 地図経路の頂点移動、線分クリックによる点追加、右クリック削除、Pによる鋭角／ベジェ切替、制御点移動。
- 同系列の端点を優先する位置スナップと、接続時のベジェ端点接線の自動調整。永続的な接続拘束は持たない。
- 記号テンプレート10種、白／黒配色、地図専用パネル。
- .mapraku JSON保存・読込、SVG／PNG出力。保存時の画像パス相対化、同一フォルダーの一時ファイルから置換保存。
- 新規／開く／終了時の変更確認。Debugだけ終了時の保存確認を省略。Debugでも新規／開くの確認は維持する。

## コメント・肥大化防止の規約

ユーザー指定の参照元：`D:\DelphiProg\test\Backupn\note.md`「コメント・肥大化防止」。このプロジェクトでも以下を適用する。

- 日本語コメントは目的・判断理由・制約を説明し、コードの言い換えは避ける。
- 各ユニット冒頭に責務を記載する。
- 中止・上書き・例外・スレッド間通信では安全上の理由を説明する。
- 1ユニットは300～500行を目安とし、500行超で分割を検討する。
- 1メソッドは50行を目安とし、責務が混ざれば行数に関係なく分割する。
- 1フォルダーは5～8ユニットを目安とし、8個超で機能別配置を検討する。
- 行数合わせの細分化や、将来用の空フォルダーは作らない。
- 日本語を含むPascalはUTF-8 BOM付き。Win64 Debug／Releaseの警告・エラー0と `git diff --check` を確認する。

### 完成機能の整理（2026-09-17）

端点接続・立体交差・近接橋の連結はユーザー確認済みの完成機能とし、描画と操作の仕様を維持して内部構成を整理した。

- 汎用レンダラーから交差描画を分離。交差準備も踏切、トンネル、橋の切り抜き、統合側線へ分けた。
- 交差判定、変換済み経路のサンプリング・弧長、橋区間統合を独立させた。基礎クラスの端点・接線規則は共通のまま維持する。
- 複数経路の仮編集と履歴を `MapRakuPathEditSession` に分離した。
- RGBAバッファと合成を `MapRakuRenderBuffer` に分離。既存の型名・呼び出しは汎用レンダラーの別名と委譲で互換を維持する。
- 回帰テストは総合、端点接続、交差描画、共通フィクスチャへ分類した。
- DPR／DPROJの明示参照・検索パスとテスト生成スクリプトを新配置へ更新した。
- 基盤由来の大きなDocument／Canvas／入力処理等は、今回の地図機能整理に合わせた行数だけの分割を行わない。今後変更するときに責務と依存関係を確認して分割する。

## 構成

- Source/Core/Model：文書・基礎経路クラス。系列判定と端点接線調整を共有。
- Source/Core/Commands/Path：複数経路を変更する操作のプレビュー復元とUndo／Redo。
- Source/Map/Crossings：交差判定、共通の経路幾何、橋区間統合。
- Source/Map/Rendering/Crossings：下側クリップと橋・トンネル・踏切の描画準備。
- Source/Map/Rendering/Paths：道路・線路・川・階段の専用描画と振り分け。
- Source/Map/Editing：経路編集と端点スナップ。
- Source/Map/Commands：経路挿入、交差関係など地図固有の履歴コマンド。
- Source/Map/UI：地図作成パネル。
- Source/Map/Symbols：通常図形と文字を組み合わせた編集可能な記号。
- Source/Map/Appearance：配色切替とUndo。
- Source/Map/Export：SVG／PNG出力、JSONファイル保存。
- Source/Rendering/Buffer：RGBAバッファ、透明度、画素合成。
- Source/Rendering/MapRakuRenderer.pas：文書描画の組み立てと既存描画API。
- Source/Shell/MapRakuMainForm.pas：ファイル操作と画面の組み立て。
- Tests/MapRakuTestCases.pas：保存・層・UI等の総合回帰テストと実行入口。
- Tests/Map：端点接続、交差描画の回帰テストと比較画像の生成。
- Tests/Support：共通検査と標準経路データ。

## ビルド・テスト

Delphi 37.0を C:\Program Files (x86)\Embarcadero\Studio\37.0 に導入した環境で、プロジェクトルートから実行する。

```powershell
.\Tools\build.ps1 -Config Debug
.\Tools\build.ps1 -Config Release
.\Tools\test.ps1
```

実行ファイルは Win64/Debug/MapRaku.exe と Win64/Release/MapRaku.exe。各フォルダーにsk4d.dllが必要で、ビルドスクリプトがコピーする。test.ps1はテスト用プロジェクトを生成し、TestOutputへ出力する。

直近のReleaseビルドは警告・エラー0。自動テストで挿入・Undo／Redo、同層交差部、層境界を挟む別高さの縁、層境界の保存復元、点編集、JSON往復と異種形式拒否、配色、10種の記号、配置終了時の選択モード、SVG／PNG、フォーム生成、未保存Debug終了の確認省略を検証済み。

実画面では通常起動、道路配置、右クリック終了を確認済み。その後に加えた頂点編集等は自動テスト中心で、実画面全操作の通し検証はまだ。TestOutput/editor.pngは非表示フォームのPaintToでは内容が描画されず、UI検証の証拠として使わない。

## 修正した不具合

TMapToolsPanelの子コンボボックス生成時に親ウィンドウがなくVCL例外になった。CreateToolsへHostを渡し、子コントロール初期化前にParentを設定するよう修正。フォーム生成テストでも例外がないことを確認した。

## 維持する仕様

経路は点ごとに直線／曲線を混在できる。配置終了で選択モードへ戻り、次の配置は独立レイヤーとなる。独立の層番号は設けず、レイヤー順と一覧内の層境界で表示順と高さを扱う。1本の道路の高さが途中で変わる場合は道路を分割して別の境界区間へ置く。空白の左ドラッグは範囲選択、中央ドラッグは移動。スナップはAltで一時解除。画像のCodexキャラクターは対象外。

## 未完了・次に確認すること

1. 最新UIで点編集、記号配置、テーマ、ファイル操作の通し確認。点不足で配置を中止する場合のツール解除、記号配置待ちのEsc解除を確認・調整。
2. 記号スナップの対象種類と向きの調整。駅の線路限定、歩道橋の幅・向きなど。
3. 外部画像の相対パス、移動後の再読込、欠落画像除外、SVG画像埋込みの回帰テスト。
4. 同層内の道路×線路、線路×線路、道路×川など種類別交差規則と、SVGの高度な効果の一致確認。
5. 交差関係とは独立して配置する橋・トンネル専用記号、縦書き、最近使ったファイルは未実装。方位等の固定色記号は暗色背景で視認性を再確認。
6. 提供された店舗略地図相当のサンプルを作り、作図全体の不足を調整する。

READMEは利用者向け、本文書は再開向け、ちずらく_仕様検討まとめ.mdは製品仕様。予定を実装済みとして書かず、変更時に関連文書を更新する。
## IDE用プロジェクト設定の修正

出力先とDCU出力先をDelphi標準の .\$(Platform)\$(Config) に統一した。MSBuildProjectDirectoryによる出力先指定、Releaseだけの独自出力変数、旧アプリのカスタムTargetを削除。DPRで明示する全ユニットをDPROJにも登録し、検索パスは自身のSource／Lib配下の相対パスに統一した（RTL/VCL等は通常のDelphi環境を使用）。Skia DLLは標準PostBuildEventでPROJECTDIRからOUTPUTDIRへコピーするため、PowerShellスクリプトの事前実行は不要。

IDEで既にプロジェクトを開いている場合、ディスクの設定を読み直すため再読み込みする。旧 Win64/MapRaku 配下の成果物は残っているが、今後の出力先は Win64/Debug または Win64/Release。
