# ちずらく 開発ノート

作業再開時に読む未完了事項と共通事項。完了済みの実装、検証、修正、設定変更は [HISTORY.md](HISTORY.md) に記録する。利用者向け説明は README.md、製品仕様は ちずらく_仕様検討まとめ.md を参照する。予定を実装済みとして書かず、変更時に関連文書を更新する。

## 未完了・次に確認すること

1. 最新UIで点編集、記号配置、テーマ、ファイル操作を通して確認する。点不足で配置を中止した場合のツール解除と、記号配置待ちのEsc解除を実画面で確認する。実画面では通常起動、道路配置、右クリック終了まで確認済みで、全操作の通し検証はまだ。`TestOutput/editor.png` は非表示フォームのPaintToで内容が描画されず、UI検証の証拠には使えない。
2. 記号スナップの対象種類と向きを調整する。駅の線路限定、歩道橋の幅・向きなど。
3. 外部画像の相対パス、移動後の再読込、欠落画像除外、SVG画像埋込みを回帰テストする。
4. 同層内の道路×線路、線路×線路、道路×川など種類別交差規則と、SVGの高度な効果の一致を確認する。
5. 交差関係とは独立して配置する橋・トンネル専用記号、縦書き、最近使ったファイルは未実装。方位等の固定色記号は暗色背景で視認性を再確認する。
6. 提供された店舗略地図相当のサンプルを作り、作図全体の不足を調整する。
7. 交差表現範囲のドラッグによる連続調整は次段階。踏切の一括表示処理は停止中で、必要性と再開条件を確認する。

## 共通の実装方針

- 道路・線路・川で同じ経路処理を重複実装しない。端点、接線、接続可否は `TVectArtPathLayer`、系列判定は `ConnectionFamilyOf` を使い、種類固有の差だけを共通の判定・拡張点へ追加する。交差、橋区間、描画、編集履歴も既存の共通処理を優先する。
- 端点接続は配置・頂点ドラッグ時の位置と接線の調整であり、経路間に永続的な追従拘束は作らない。同系列の端点を優先し、Altでスナップを一時解除する。ロックされた相手は位置参照のみとする。
- 立体交差では上側を連続させ、下側の線・縁・模様を交差区間と余白にわたり描画対象から除外する。上塗り、白塗り、合成済み画面の消去で代用しない。画面・PNG・SVGに同じ規則を適用する。平面交差は継ぎ目なく接続し、踏切は線路模様を優先する。
- 同じ上側経路・同じ側線形式の橋区間が重なるか十分近い場合は側線を連結し、外端だけに開きを付ける。離せば分離する。下側の切り抜きは交差ごとに維持する。
- 経路は点ごとに直線／曲線を混在できる。配置終了で選択モードへ戻り、次の配置は独立レイヤーとなる。高さは独立した層番号ではなくレイヤー順と層境界で扱う。グループは整理専用。一本の道路の高さを途中で変える場合は道路を分割する。空白の左ドラッグは範囲選択、中央ドラッグは移動。画像のCodexキャラクターは対象外。
- 選択済みの地図経路は線上の左ドラッグで移動する。右クリックメニューの「頂点」→「追加／削除」はクリック位置に応じて有効化し、追加は線上、削除は既存頂点上で実行する。既存頂点・ベジェハンドルの左ドラッグ編集とPによる種類切替は維持する。
- 色の種類別設定はキャンバス設定の「色」ページとJSONのキャンバス設定に置く。道路・川の新規配置色はそれぞれのプリセットを使い、既存経路への一括反映は明示操作で個別色も含める。ロック中の経路は変更しない。選択中の同種経路1件に個別色があれば、その配置に限って色を引き継ぐ。JR・私鉄の本体／模様色は種類全体の表示設定とし、既存経路にも反映する。白／黒テーマは未変更の標準色だけ切り替え、手動設定色は維持する。ツールの色表示と作図プレビューは実際の配置色へ合わせる。
- 通常の未指定部分は実装側で判断し、重要な矛盾のみ確認する。既存の保存値との互換、Undo／Redo、JSON保存を意識する。

## 参照元と構成

`D:\DelphiProg\test\SYNC_ScreenLayout` の `ScreenDesignMaker.dpr`／`.dproj`（単独デバッグアプリ）と `SYNC_ScreenLayout_Filter.dpr`／`.dproj`（AviUtl2プラグイン）をUI、編集、描画、保存、Undo／Redoの参考にする。参照元は変更せず、MapRakuは単独アプリとして分離を維持する。プラグイン固有のホスト連携は持ち込まない。

- `Source/Core/Model`：文書と基礎経路。`Source/Core/Commands/Path`：複数経路の編集履歴。
- `Source/Map/Crossings`：交差判定と経路幾何。`Source/Map/Rendering/Crossings`：下側クリップと交差記号。
- `Source/Map/Rendering/Paths`：種類別経路描画。`Source/Map/Editing`：経路編集と端点スナップ。`Source/Map/Commands`：地図固有の履歴。
- `Source/Map/UI`、`Symbols`、`Appearance`、`Export`：作成パネル、記号、配色、SVG／PNG／JSON。
- `Source/Rendering/Buffer`：RGBA合成。`Source/Rendering/MapRakuRenderer.pas`：文書描画の組み立て。`Source/Shell/MapRakuMainForm.pas`：ファイル操作と画面。
- `Tests/MapRakuTestCases.pas`、`Tests/Map`、`Tests/Support`：総合・地図機能テストと共通フィクスチャ。

## コメント・肥大化防止の規約

参照元：`D:\DelphiProg\test\Backupn\note.md`「コメント・肥大化防止」。

- 日本語コメントは目的・判断理由・制約を説明し、コードの言い換えは避ける。各ユニット冒頭に責務を記載する。中止・上書き・例外・スレッド間通信では安全上の理由を説明する。
- 1ユニット300～500行、1メソッド50行、1フォルダー5～8ユニットを目安とする。責務が混ざれば行数に関係なく分割を検討する。行数合わせの細分化や将来用の空フォルダーは作らない。
- 日本語を含むPascalはUTF-8 BOM付き。Win64 Debug／Releaseの警告・エラー0と `git diff --check` を確認する。

## ビルド・テスト

Delphi 37.0（`C:\Program Files (x86)\Embarcadero\Studio\37.0`）の環境で、プロジェクトルートから実行する。

```powershell
.\Tools\build.ps1 -Config Debug
.\Tools\build.ps1 -Config Release
.\Tools\test.ps1
```

出力は `Win64/Debug/MapRaku.exe` と `Win64/Release/MapRaku.exe`。各フォルダーに `sk4d.dll` が必要で、ビルド時にコピーされる。`test.ps1` はテスト用プロジェクトを生成し、`TestOutput` に出力する。IDEで以前からプロジェクトを開いている場合は、ディスク上の設定を再読み込みする。
