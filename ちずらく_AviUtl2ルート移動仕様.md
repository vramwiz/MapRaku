# ちずらく 発展形検討
## AviUtl2プラグイン版：ルート移動・地図追従表示

## 1. 基本方針

「ちずらく」は本来、独立した案内図作成アプリとして開発する。

他のプラグインでは、AviUtl2プラグインが本体であり、デバッグ用としてアプリ版を用意する場合があるが、
今回は逆である。

- アプリ版が本体
- AviUtl2プラグイン版は発展形
- プラグイン化する明確な意味がなければ実装しない

AviUtl2側で単に地図を表示するだけでは、SVGや画像として読み込めば済むため価値は低い。

プラグイン版では、動画用途に特化した以下の機能を持たせることで差別化する。

- 地図上の現在位置表示
- ルートに沿ったマーカー移動
- 移動軌跡表示
- マーカーに追従する地図スクロール
- 地図端でのスクロール制御
- 層情報を利用した高架・トンネル等の前後関係表現

主な想定用途は以下。

- 旅動画
- 街歩き動画
- 鉄道・道路解説
- 地理・歴史解説
- 店舗・イベント会場案内
- RPG風の現在地表示

---

## 2. アプリ版とプラグイン版の役割

### アプリ版

地図を作成し、動画用の移動ルートを編集する。

- 道路
- 線路
- 建物
- 河川
- 各種地図オブジェクト
- 層
- 高架
- トンネル
- 踏切
- 移動ルート

を編集する。

さらに、ルート移動のプレビューもアプリ上で確認できるようにする。

### AviUtl2プラグイン版

アプリ側で作成した地図データを利用し、動画時間軸上でマーカーを移動させる。

AviUtl2側では複雑な座標指定を行わせず、

**進行位置 0.00 ～ 100.00**

という1つのパラメータを動かすだけで、
始点から終点までマーカーが移動することを基本とする。

---

## 3. ルートオブジェクト

動画用として、新たに「ルート」を追加する。

ルートは道路そのものではなく、
道路・線路等の上に重ねて配置する専用オブジェクトとする。

### 基本仕様

- 基本は連続直線
- 道路と同様の頂点編集機能を持つ
- 必要に応じてベジェ曲線も利用
- 色を持つ
- 太さを持つ
- 動画上では移動軌跡として表示可能
- 地図上の道路や線路とは独立したオブジェクト

道路自体に移動属性を持たせず、
移動ルートを独立させることで、

- 往路
- 復路
- 別日の移動
- 別キャラクター
- 別ルート

を同一地図上に作成できるようにする。

---

## 4. ルートは複数区間で構成する

1本のルートを、必ずしも1つの線オブジェクトとして保持しない。

理由は以下。

- 鋭角連続線とベジェ曲線を切り替える場合がある
- 層が途中で変わる
- 高架・地下・トンネル等により描画順が変化する
- 1本の連続線だけでは表現できない

そのため、

**1つの論理的なルート**
を
**複数のルート区間**
で構成する。

例：

- 区間A：鋭角連続線・層1
- 区間B：ベジェ曲線・層1
- 区間C：鋭角連続線・層0
- 区間D：ベジェ曲線・層1

ユーザーからは、これらを1本のルートとして扱う。

---

## 5. ルート区間の接続判定

分断されたルートオブジェクトを、
正しく1本のルートとして認識する必要がある。

### 基本条件

ルート区間同士は、
端点座標が十分近いことを前提とする。

判定対象は途中の頂点ではなく、

- 始点
- 終点

のみとする。

### 接続判定で利用する要素

- 端点同士の距離
- 区間の進行方向
- 所属ルート
- 層の遷移
- 接続候補数

近接候補が1つだけなら自動接続できる。

候補が複数存在する場合は、
自動決定せず確認または警告を行う。

---

## 6. 始点・終点

単純なルートであれば、
一筆書きのように接続関係から始点と終点を求められる可能性がある。

ただし、

- 分岐
- ループ
- 近接した別ルート
- 折返し

などでは曖昧になる。

そのため必要に応じて、
ルート分類内に以下の編集用オブジェクトを用意する。

- 始点オブジェクト
- 終点オブジェクト

これらは動画上には表示しない。

始点から接続区間を順番にたどり、
終点まで到達できるかを確認する。

### エラーとして扱うもの

- 接続不能
- 分岐が複数存在
- 終点へ到達不能
- 途中でルートが途切れる
- 未使用区間が存在

---

## 7. 進行位置

AviUtl2側では、
ルート上の移動を1つのパラメータで制御する。

### 進行位置

**0.00 ～ 100.00**

- 0.00 = 始点
- 100.00 = 終点

小数点第2位まで利用可能とする。

実質10,001段階となり、
動画上の移動として十分な細かさを確保できる。

### 移動距離

頂点番号基準ではなく、
ルート全体の実距離基準で計算する。

そのため、

- 点間隔が不均一
- ベジェ区間が存在
- 複数区間をまたぐ

場合でも、不自然な速度変化を抑える。

---

## 8. マーカー

マーカーは現在位置を示す。

### 外部参照

実際の動画では、
外部画像ファイルを指定できるようにする。

用途例：

- 人物
- キャラクター
- 車
- 電車
- 矢印
- 独自アイコン

### 内蔵マーカー

外部ファイルが未指定の場合は、
内蔵マーカーを選択できるようにする。

内蔵マーカーは図形として扱い、
色変更可能とする。

### 編集画面のプレビュー

アプリ側では外部参照ファイルを必須とせず、
プレビュー用の図形マーカーを使用できるようにする。

必要であれば標準マーカー素材を同梱してもよい。

---

## 9. マーカーオフセット

外部画像では、
画像中心とルート上の接地点が一致しない場合がある。

例：

- 人物画像では足元をルート上に置きたい
- ピンでは先端を現在位置に置きたい
- 車では中心位置を調整したい

そのため以下を用意する。

- マーカーオフセットX
- マーカーオフセットY

初期値は0。

マーカーオフセットは、
あくまで画像表示位置の補正とする。

ルート上の論理的な現在位置、
地図スクロール、
クリッピング計算には影響させない。

---

## 10. マーカー回転

マーカーの向きを制御する。

真偽値ではなく、
将来拡張できるセレクタ形式とする。

### マーカー回転

候補：

- 固定
- 進行方向
- 進行方向＋回転補正

### 回転補正角度

画像自体の正面方向が、
ルート進行方向と一致しない場合に補正する。

---

## 11. 層と高低差

既存の地図データには層の概念がある。

これを移動アニメーションにも利用する。

ルート区間ごとに層を持たせ、
軌跡の描画順と移動マーカーの交差クリップを現在の区間に合わせる。

### 表現できるもの

- 高架下通過
- オーバーパス
- アンダーパス
- トンネル
- 地下道
- 跨線橋
- 階段による層変更

### 高架下等の処理

マーカーが上層オブジェクトの下を通る場合、

- 道路と共通の交差判定・切り抜き領域を使用する
- マーカーと方向ポインターのうち、上側の交差領域と余白に重なる部分へ「道路の下」の透明度を適用する（初期値100で完全透明）
- 軌跡は各ルート区間の高さとクリップに従う
- 上側の上塗りや、合成済み地図の消去で代用しない

移動マーカーは軌跡に覆われないよう最後に合成するが、その前の描画段階で現在区間のクリップを適用する。Spaceプレビューにも同じ規則を適用する。

これにより、
単純な2D上の移動ではなく、
地図構造に沿った前後関係を表現する。

---

## 12. 軌跡

マーカーが移動した部分を、
ルートの軌跡として表示できるようにする。

ルート線自体が以下を持つ。

- 色
- 太さ

プレビューおよびAviUtl2上では、
進行済み部分を軌跡として表示する。

高架下やトンネル等では、
層情報に従って軌跡の前後関係も制御する。

---

## 13. 編集画面プレビュー

アプリ編集画面にルートプレビュー機能を実装する。

### 起動方法

- ポップアップメニューから「プレビュー」
- ショートカット：Space

### 動作

- 始点から終点までマーカーが移動
- 軌跡を残す
- 鋭角連続線とベジェ曲線を連続して移動
- 区間ごとの層を反映
- 高架下ではマーカーが隠れる
- トンネル等も表示制御
- 終点で停止

Spaceキーは、
再生・停止の切替として利用する。

プレビューは単なる経路確認ではなく、
AviUtl2上での最終表示に近い状態を確認するためのものとする。

---

## 14. RPG風地図表示

AviUtl2側では、
地図全体を常に表示するのではなく、
指定範囲のみを切り出して表示できるようにする。

フィルターとしてのクリッピングではなく、
地図表示そのものの機能として実装する。

### 表示サイズ

- 表示幅
- 表示高さ

単位はpx。

この矩形範囲だけを地図として表示する。

---

## 15. マーカー追従スクロール

RPGのマップ表示のように、
マーカー移動に合わせて地図をスクロールさせる。

基本的には、

- マーカーは表示枠内を移動
- 一定範囲を超えると地図側がスクロール
- 地図端ではスクロールを停止
- 地図が欠けて表示されないようにする

という挙動とする。

地図端では、
地図位置を表示可能範囲に固定し、
マーカー側が表示枠内を移動する。

---

## 16. スクロール開始率

表示幅・表示高さはユーザー設定値のため、
スクロール開始位置を固定pxでは設定しにくい。

そのため割合で指定する。

### スクロール開始率

初期値：

**25%**

表示枠の端から25%内側を、
スクロール開始境界とする。

つまり中央50%程度の範囲では、
マーカーだけが移動する。

その範囲を超えたら、
地図が追従スクロールする。

表示幅・高さが変わっても、
同じ感覚で利用できることを目的とする。

初期値だけでもそれなりに自然に動き、
違和感がある場合のみユーザーが調整する設計とする。

---

## 17. AviUtl2側 設定パラメータ

以下の順序を基本とする。

- 編集
- 進行位置 0.00～100.00

### マーカー

- マーカー画像ファイル
- マーカー選択
- マーカー色
- マーカー拡大縮小
- マーカー透明度：0～100、初期値0。0＝不透明、100＝完全透明
- マーカー透明度（道路の下）：0～100、初期値100。通常値とは独立して、共通交差領域と余白に重なる部分へ適用する。方向ポインターも共用し、軌跡の切り抜きは変えない
- マーカーオフセットX
- マーカーオフセットY
- マーカー回転
- 回転補正角度

### 表示

- 表示幅
- 表示高さ
- スクロール開始率

- 地図データJSON

### 地図データJSON

地図データはJSON文字列としてAviUtl2オブジェクト内に保持する。

通常ユーザーが直接編集する必要はないため、
UI上では見えない扱いとし、
設定項目の最下部に配置する。

---

## 18. 編集ボタン

AviUtl2側に「編集」ボタンを配置する。

編集ボタンから地図編集画面を開き、

- 地図編集
- ルート編集
- 始点・終点設定
- プレビュー確認

を行う。

編集結果はJSONとしてAviUtl2側へ戻す。

---

## 19. プラグイン版の価値

単に地図を描けるだけでは、
AviUtl2プラグイン化する意味は薄い。

価値の中心は、

**地図データがそのままモーションパスとして使えること**

にある。

さらに、

- 進行位置1つだけで移動
- ルートに沿って自動移動
- マーカー自動回転
- 軌跡表示
- 層に応じた遮蔽
- RPG風の追従スクロール
- 地図端処理

まで自動化できれば、
AviUtl2標準機能だけで手作業する場合との差が大きくなる。

役割としては、

**アプリ版：案内図と移動ルートを作る**

**AviUtl2版：その案内図の中を移動して見せる**

という分担とする。
